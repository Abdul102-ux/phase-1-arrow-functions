const divide = (parameter1,parameter2) => { 
console.log(`dividing ${parameter1}`);
console.log(`dividing ${parameter2}`);
return parameter1 / parameter2;
}
divide("1 / 5")

const square = (num) => {
    return num * num;
}

const add =(a,b) => a + b;

